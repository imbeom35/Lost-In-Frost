encrypt:
  key-store:
    location: file:/apiEncryptionKey.jks
    alias: [별칭]
    password: [비밀번호]

spring:
  cloud:
    config:
      uri: http://localhost:9999
      name: db, eureka-service, gateway, jwt, log, payment, rabbitmq, redis, s3, swagger, value
