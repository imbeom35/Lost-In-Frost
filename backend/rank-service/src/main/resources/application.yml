server:
  port: 0

spring:
  application:
    name: rank-service

management:
  endpoints:
    web:
      exposure:
        include: health, bus-refresh, prometheus, metrics

springdoc:
  api-docs:
    version: openapi_3_1
    enabled: true
#    path: /rank-service/v3/api-docs
#    path: /v3/api-docs

